# Current Feature

## Status
Not Started

## Goals
<!-- Add goals for the current feature here -->

## Notes
<!-- Add notes and context for the current feature here -->

## References


## History
- **2026-06-20: Docs and Screenshot Refresh**: Updated documentation files (README, CONTEXT, split-project-overview) to reflect shipped features (Solo Mode, Backup/Restore, Dashboard Filters, Recharts) and replaced dashboard screenshots with the latest UI.
- **2026-06-20: Deepen Sales Log**: Refactored the Weekly Sale domain into a cohesive `SalesLog` module. Absorbed and deleted shallow `analytics.ts` and `insights.ts` modules. Consolidated all array-iteration logic and tests into a single, unified interface for the dashboard and AI agent.
- **2026-06-19: Marketing Site**: Created a static, offline-capable marketing landing page deployed via Cloudflare Pages. Included custom SEO configuration, semantic HTML5, smooth scrolling, and app screenshots.
- **2026-06-19: Application Icon Update**: Extracted and configured a new pie chart application icon for both Next.js App Router and the native Electron Builder packager, ensuring the icon reflects in taskbars, shortcuts, and installer files.
- **2026-06-18: Analytics Dashboard Improvements**: Implemented a global type filter (All, Solo, Split) integrated across the data layer and UI. Refactored Summary Cards to dynamically adapt their CSS grid layout and conditionally hide Partner Share when operating solo. Enhanced Monthly and Yearly Performance charts to use stacked bars with overlaid Gross Sales lines, ensuring gapless continuous years in the Yearly chart spanning up to a 12-year window by default. Preserved strict standard financial color-coding aesthetics and fixed TypeScript prop definitions.
- **2026-06-18: Year-by-Year Backup System**: Updated the backup system to strictly operate on a year-by-year basis. Export operations now target specific selected years, preventing enormous JSON file generation. Import operations now dynamically identify the target year and safely restrict any overwrite operations exclusively to that targeted year's data, protecting other historical data.
- **2026-06-10: 100% Test Coverage & CI Validation**: Achieved 100% test coverage across the application. Simplified sorting logic in analytics domain. Fixed ESLint and Typecheck warnings to ensure a clean CI pipeline.
- **2026-06-03: Include User Preferences in Backup**: Updated the data export functionality to package user settings (default split percentage, AI model) safely alongside sales records in the JSON backup. Updated the import logic to gracefully handle both the new structured format and legacy array backups.
- **2026-06-02: 100% Solo Mode Option**: Implemented a "Solo Mode" for sales where a 100% split seamlessly adapts the UI (hiding partner shares, renaming owner fields) and the AI entry agent, without requiring schema changes or new accounts. Added test coverage to `sales-actions.test.ts`.
- **2026-06-01: Deepen Weekly Sale Module & Native Binding Automation**: Implemented isomorphic WeeklySale domain entity factory for unified validation and deep data structures, refactoring the data layer and AI entry agent. Automated native C++ module (SQLite) bindings via package.json scripts to seamlessly handle ABI mismatches between Node and Electron.
- **2026-06-01: AI Security Audit & Codebase Cleanup**: Implemented robust prompt injection prevention in the AI entry agent, added comprehensive encryption tests for local AI settings, refactored Prisma seed data to use randomized generation, cleaned up unused imports/variables, and updated documentation for Electron packaging.
- **2026-05-30: ASAR Packaging & NSIS UX Improvements**: Enabled `asar` packaging in `electron-builder` to compress application files, significantly improving Windows installation and boot times while avoiding 260-character path limits. Configured `asarUnpack` to safely extract native binaries (`**/*.node`). Switched NSIS configuration to an assisted wizard to let users control installation directories and auto-launching. Added a custom `uninstaller.nsh` macro to prompt users before wiping their `AppData\Roaming\Split` folder during uninstallation.
- **2026-05-30: Fixed Production Database Leaks**: Prevented the local `dev.db` from being bundled in the production Electron application. Reconfigured the postbuild pipeline to generate an empty `template.db` using the Prisma CLI, which the application now safely copies to the user's local AppData directory on first launch.
- **2026-05-30: Performance & Architecture Refactor**: Overhauled data layer with non-blocking crypto, optimized Prisma queries and analytics loops, and resolved Next.js App Router waterfalls using Promise.all. Optimized React component render cycles with useMemo and hoisted regex. Fixed Electron native module packaging (better-sqlite3) via a dynamic postbuild script patching package.json for electron-builder. Resolved duplicate Vitest test executions.
- **2026-05-29: Dependency Cleanup and Electron Crash Fixes**: Cleaned up stale dependencies, updated packages to resolve 9 deprecation warnings and all high-severity vulnerabilities. Configured `vitest` correctly with Next.js 16 constraints. Explicitly mapped Auth.js URLs in the Electron main process to fix silent 404s in the bundled React application. Updated GitHub Actions workflows to use Node 24 and explicitly rebuild native dependencies for Electron packaging.
- **2026-05-29: Electron App Packaging & GitHub Actions Automation**: Configured electron-builder to package the Next.js app into a native Windows executable (.exe). Renamed the application from SplitLedger to Split. Fixed lingering type and lint issues. Automated the entire distribution pipeline by creating a GitHub Actions release workflow and setting up a patch-package step to properly bundle native SQLite dependencies in the cloud.
- **2026-05-28: Turbopack, Prisma 7, NextAuth v5 bugfixes**: Fixed Prisma 7 client generator resolution in Next.js Turbopack by explicitly defining `.prisma/client` path and bypassing default proxy for TS strict bundler mode. Fixed NextAuth v5 server action "swallowed 303 redirect" bug during `react-hook-form` fetches by defaulting back to client-side `signIn` flow. Cleaned up Auth UI to original Shadcn design.
- **2026-05-28: Migrate to SQLite, Local AI Settings, and Open-Source Release**: Transitioned from Neon PostgreSQL to a purely local SQLite architecture. Removed Cloudflare Turnstile and Resend dependencies. Implemented a fully localized AI settings interface in the profile page, allowing users to configure custom OpenRouter API keys safely stored locally. Updated documentation, test suite, and open-source configurations.
